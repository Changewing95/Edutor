#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<<< HEAD:Edutor/node_modules/.bin/sshpk-conv
  exec "$basedir/node"  "$basedir/../sshpk/bin/sshpk-conv" "$@"
else 
  exec node  "$basedir/../sshpk/bin/sshpk-conv" "$@"
========
  exec "$basedir/node"  "$basedir/../qrcode/bin/qrcode" "$@"
else 
  exec node  "$basedir/../qrcode/bin/qrcode" "$@"
>>>>>>>> master:Edutor/node_modules/.bin/qrcode
fi
